---
import BaseLayout from "../layouts/BaseLayout.astro";
import PageHero from "../components/PageHero.astro";
import { team } from "../data/team";
import { site } from "../data/site";

const initials = (name: string) =>
  name
    .split(/\s+/)
    .filter(Boolean)
    .slice(0, 2)
    .map((s) => s[0]?.toUpperCase() ?? "")
    .join("");
---

<BaseLayout
  title="Zespół · Medi3"
  description="Poznaj zespół Medi3 — położne, fizjoterapeuci, neurologopedzi. Doświadczenie i ciepłe podejście do pacjentki."
>
  <PageHero
    eyebrow="Ludzie Medi3"
    title="Poznaj nasz zespół"
    subtitle="Położne, fizjoterapeuci i neurologopedzi — pracujemy zgrani, żeby kompleksowo zaopiekować się Tobą i Twoim dzieckiem."
  />

  <section class="container-x py-16">
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {team.map((m) => (
        <article class="group relative overflow-hidden rounded-3xl border border-salmon-100 bg-blush-100 transition hover:shadow-xl hover:-translate-y-0.5">
          <div class="aspect-[4/5] relative overflow-hidden bg-cream-100">
            {m.photo ? (
              <img src={m.photo} alt={`${m.name} — ${m.role}`} class="absolute inset-0 w-full h-full object-cover" loading="lazy" />
            ) : (
              <div class="absolute inset-0 flex items-center justify-center">
                <span class="font-serif text-6xl text-salmon-400/70 select-none">{initials(m.name)}</span>
              </div>
            )}
            <div class="absolute top-4 left-4 inline-flex items-center gap-1.5 rounded-full bg-white/85 backdrop-blur px-2.5 py-1 text-[10px] font-semibold uppercase tracking-wider text-salmon-700">
              {m.role.split("·")[0].trim()}
            </div>
          </div>

          <div class="p-6 bg-blush-100">
            <h3 class="font-serif text-xl text-salmon-900 leading-tight">{m.name}</h3>
            <p class="mt-1 text-sm text-salmon-700/80">{m.role}</p>

            {m.specs.length > 0 && (
              <ul class="mt-4 flex flex-wrap gap-1.5">
                {m.specs.map((s) => (
                  <li class="text-[11px] rounded-full bg-white/70 text-salmon-700 px-2.5 py-1 border border-salmon-200/60">{s}</li>
                ))}
              </ul>
            )}

            <p class="mt-4 text-sm text-salmon-900/70 leading-relaxed">{m.bio}</p>
          </div>
        </article>
      ))}
    </div>

  </section>

  <!-- CTA -->
  <section class="container-x pb-20">
    <div class="rounded-[2rem] bg-gradient-to-br from-salmon-500 via-salmon-600 to-peach-600 px-8 py-12 sm:px-12 sm:py-16 text-white relative overflow-hidden">
      <div class="absolute -right-20 -top-20 h-72 w-72 rounded-full bg-white/10 blur-3xl"></div>
      <div class="relative max-w-2xl">
        <h2 class="font-serif text-3xl sm:text-4xl tracking-tight">Chcesz dołączyć do zespołu?</h2>
        <p class="mt-3 text-white/85">Szukamy specjalistek i specjalistów z pasją do pracy z kobietami i rodzinami. Napisz do nas.</p>
        <div class="mt-7 flex flex-wrap gap-3">
          <a href={`mailto:${site.email}`} class="inline-flex items-center gap-2 rounded-full bg-white text-salmon-700 px-6 py-3 text-sm font-medium hover:bg-cream-50">
            {site.email}
          </a>
          <a href={`tel:${site.phoneTel}`} class="inline-flex items-center gap-2 rounded-full border border-white/40 px-6 py-3 text-sm font-medium hover:bg-white/10">
            ☎ {site.phone}
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
